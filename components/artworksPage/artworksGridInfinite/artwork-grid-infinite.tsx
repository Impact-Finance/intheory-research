import styles from './artwork-grid-infinite.module.scss';

const ArtworkGridInfinite = () => {
  return (
    <section className={styles.section}>
      <div className={styles.gridBox}>artwork grid</div>
    </section>
  );
};

export default ArtworkGridInfinite;
